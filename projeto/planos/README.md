# Planos de implementação

Planos bite-sized produzidos pela skill `writing-plans` e executados pela skill `executing-plans`.

- **Nome:** `AAAA-MM-DD-<nome-da-feature>.md`
- **Fluxo:** alinhar o desenho com o Eduardo → escrever o plano aqui → executar tarefa a tarefa, verificando no browser (porta 5500, dark+light) → fechar com `eqtl-checkpoint`.
- Git é do Eduardo: o agente descreve os commits, ele executa.

Ainda não há planos escritos. O primeiro candidato é a Fase 5 (EY): ver `../Itens/I-0001..I-0003` e `../../tools/ey_IMPLEMENTACAO.md`.
